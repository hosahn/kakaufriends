import styled from 'styled-components';

export const MiainChallengeContainer = styled.div`
  width: 70rem;
  padding: 10px;
  background: rgba(243, 240, 255, 0.5);
  box-shadow: 1px 3px 1px #dadce0;
  border-radius: 20px;
`;

export const Background = styled.div`
  height: 100%;
  width: 100%;
  z-index: 90;
  display: grid;
  place-items: center;
  position: fixed;
  left: 0;
  top: 0;
  text-align: center;
  background-color: rgba(0, 0, 0, 0.5);
`;